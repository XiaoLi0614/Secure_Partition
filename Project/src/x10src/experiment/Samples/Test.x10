//import p;

public class Test {	
	public static def main(args: Rail[String]): Void {
	    val a = new A();
	    Console.OUT.println("Hi");
	    
	}
}


/*
		for (p in a) {
			//val v: Int = a(p);
			//finish {
				
				//val c: Array[Int]{(rank == 1)} = a;
				//async (a.dist(p)) {
					//val v: Int = c(p);
					//val v: Int = a(p);
				//}
			//}
		}

*/

