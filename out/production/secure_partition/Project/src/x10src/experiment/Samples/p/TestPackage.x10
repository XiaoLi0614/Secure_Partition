//package p;


class A { val i = 2; }



/*
		for (p in a) {
			//val v: Int = a(p);
			//finish {
				
				//val c: Array[Int]{(rank == 1)} = a;
				//async (a.dist(p)) {
					//val v: Int = c(p);
					//val v: Int = a(p);
				//}
			//}
		}

*/

